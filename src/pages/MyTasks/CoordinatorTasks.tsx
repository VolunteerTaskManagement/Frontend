import TabBarLayout from '../../layouts/TabBarLayout';
import CoordinatorTasksContent from '../../components/coordinatorTasks/CoordinatorTasksContent';

const CoordinatorTasks = () => {
  return (
    <TabBarLayout>
      <CoordinatorTasksContent />
    </TabBarLayout>
  );
};

export default CoordinatorTasks;